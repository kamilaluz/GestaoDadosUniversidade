@model DadosUniversitarios.Models.Disciplina

@{
    ViewData["Title"] = "Detalhes da Disciplina";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main class="container py-6">
    <!-- Título da Página -->
    <div class="text-center mb-5">
        <p class="text-muted">Detalhes da Disciplina</p>
        <h1 class="h3 font-weight-bold text-dark">@Model.Nome</h1>
    </div>

    <!-- Botões de Ações -->
    <div class="d-flex justify-content-between mb-4">
        <!-- Botão Voltar -->
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
            <i class="fas fa-arrow-left"></i> Voltar
        </a>
    </div>

    <!-- Cursos -->
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h5 class="card-title">Cursos</h5>
            @if (Model.Cursos.Any())
            {
                <table class="table table-striped table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th scope="col">Nome</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var curso in Model.Cursos)
                        {
                            <tr>
                                <td>@curso.Nome</td>                                
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="text-muted">Nenhum curso vinculado a esta disciplina.</p>
            }

        </div>
    </div>
    <br />
    <br />
    <!-- Professores -->
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h5 class="card-title">Professores</h5>
            @if (Model.Pessoas.Any(p => p.Tipo.NomeTipo == "Professor"))
            {
                <table class="table table-striped table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th scope="col">Nome</th>
                            <th scope="col" class="text-center" width="200px">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var professor in Model.Pessoas.Where(p => p.Tipo.NomeTipo == "Professor"))
                        {
                            <tr>
                                <td>@professor.Nome</td>
                                <td class="text-center">
                                    <!-- Formulário para exclusão -->
                                    <form asp-action="Delete" method="post" style="display:inline;"
                                          onsubmit="return confirm('Tem certeza que deseja excluir este curso?');">
                                        <button type="submit" class="btn btn-outline-danger btn-sm">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            }

            else
            {
                <p class="text-muted">Nenhum professor vinculado a esta disciplina.</p>
            }
        </div>
    </div>
    <br />
    <br />
    <!-- Alunos -->
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h5 class="card-title">Alunos</h5>
            @if (Model.Pessoas.Any(p => p.Tipo.NomeTipo == "Aluno"))
            {
                <table class="table table-striped table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th scope="col">Nome</th>
                            <th scope="col" class="text-center" width="200px">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var aluno in Model.Pessoas.Where(p => p.Tipo.NomeTipo == "Aluno"))
                        {
                            <tr>
                                <td>@aluno.Nome</td>
                                <td class="text-center">
                                    <!-- Formulário para exclusão -->
                                    <form asp-action="Delete" method="post" style="display:inline;"
                                          onsubmit="return confirm('Tem certeza que deseja excluir este curso?');">
                                        <button type="submit" class="btn btn-outline-danger btn-sm">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            }

            else
            {
                <p class="text-muted">Nenhum aluno vinculado a esta disciplina.</p>
            }
        </div>
    </div>
    <br />

</main>
